# 原始 prompt

> 持续迭代完善 CheckedBoundsPass，但每轮只能基于真实证据扩展，不做猜测式放宽。完成一轮可验证的改进

# 本轮问题

重新统计当前状态：

- pattern manifest 还有 15 个 checked-bounds 弱 oracle。
- 最新 apehex batch636 + batch635：50 个 `.ll`，checked-bounds 为 0，没有 skip。
- pattern suite 当前仍只有一个 checked-bounds skip：`0334_19494307_668d201319_1354ce2e324d`，`panic_assert` / `unrecognized_operands`。

`panic_assert` 仍按上一轮结论保留，不 rewrite。Solidity `assert` 会生成 `Panic(1)`，不能证明是 compiler bounds guard。

# 选择

本轮不改 matcher，只补两个真实 Solidity-generated case 的强 oracle：

- `0175_19493428_d314d380f5_900e60ffbb10`：9 个 checked-bounds，0 skip。
- `0176_19493428_12f263f8c9_ce83d7b0b2a7`：9 个 checked-bounds，0 skip。

选择原因：

- 它们是当前弱 oracle 中数量最高的两个 case。
- 真实输出已经有 semantic helper 和 CFG rewrite。
- 不涉及业务分支判断，rewrite 风险为 0。

不做：

- 不扩展 matcher。
- 不 rewrite `panic_assert`。
- 不一次补完所有弱 oracle。

# 修改

- `test/evm/solidity-patterns/manifest.json:3990`：给 `0175_19493428_d314d380f5_900e60ffbb10` 增加 checked-bounds kinds、panic codes、semantic markers、skip reasons 和 CFG rewrite oracle。
- `test/evm/solidity-patterns/manifest.json:4062`：给 `0176_19493428_12f263f8c9_ce83d7b0b2a7` 增加同样 oracle。

涉及函数：无。只改测试 manifest。

两个 case 的真实值相同：

- kinds：`array_bounds_memory:3`，`checked_add:2`，`checked_mul:1`，`memory_allocation_bounds:1`，`memory_allocation_pointer_bounds:1`，`checked_div:1`。
- panic codes：`50:3`，`17:3`，`65:2`，`18:1`。
- semantic markers：9。
- skip reasons：0。
- CFG rewrites：9。

# 验证

- `python3 -m json.tool test/evm/solidity-patterns/manifest.json >/tmp/notdec-pattern-manifest-round79.json`：通过。
- 两 case 临时 manifest：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest /tmp/notdec-pattern-round79-two-abs.json --project-root . --workdir /tmp/notdec-patterns-cb-round79-two-abs-final`：2 passed，0 failed。
- `cmake --build ./build --target all`：通过，`ninja: no work to do.`。
- pattern suite：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-patterns/manifest.json --project-root . --workdir /tmp/notdec-patterns-cb-round79-suite`：90 passed，0 failed。
  - checked-bounds：775；semantic markers：774；CFG rewrites：774；skip：`unrecognized_operands:1` / `panic_assert:1`。
- rewrite suite：
  - `python3 test/run_evm_solidity_rewrite_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-rewrite/manifest.json --project-root . --workdir /tmp/notdec-rewrite-cb-round79-suite`：65 passed，0 failed。
  - checked-bounds：626；semantic markers：625；CFG rewrites：625；skip：`unrecognized_operands:1` / `panic_assert:1`。
- CTest：
  - `ctest --test-dir build -R 'notdec.evm.solidity_patterns|notdec.evm.solidity_rewrite' --output-on-failure`：2/2 passed，179.12 秒。

# 当前状态

- matcher 本轮未变。
- pattern manifest 剩余 checked-bounds 弱 oracle 从 15 个降到 13 个。
- pattern/rewrite suite 的唯一 skip 仍是 `0334` 的 `panic_assert`，当前不安全 rewrite。

# 下一轮候选

按真实 case 数量、语义确定性、rewrite 风险、测试成本排序：

1. `0118_19493248_593c688d00_799916048038`：8 个 checked-bounds；语义确定；rewrite 风险低；测试成本低。
2. 7 个 checked-bounds 档位：`2002`、`1915`、`1490`、`0870`、`0657`、`0637`、`0195`、`0128`；真实 case 多；语义确定；rewrite 风险低；测试成本低。
3. `0178`、`0031`、`0460`、`0011_multi_public`：数量较低；语义确定；rewrite 风险低；测试成本低。
4. 继续扫 apehex 新 batch。真实 case 数量未知；语义确定性取决于命中形状；rewrite 风险未知；测试成本低。
5. 归档 `panic_assert` 原始合约/Yul 形状。真实 case 1 个；语义确定性低；rewrite 风险高；测试成本中等。
