# 原始 prompt

> 持续迭代完善 CheckedBoundsPass，但每轮只能基于真实证据扩展，不做猜测式放宽。完成一轮可验证的改进

# 本轮问题

重新统计当前状态：

- pattern manifest 还有 11 个 checked-bounds 弱 oracle。
- apehex batch637 + batch636：55 个 `.ll`，checked-bounds 为 0，没有 skip。
- pattern/rewrite suite 仍只有一个 checked-bounds skip：`0334_19494307_668d201319_1354ce2e324d`，`panic_assert` / `unrecognized_operands`。

`panic_assert` 仍保留，不 rewrite。Solidity `assert` 会生成 `Panic(1)`，当前不能证明它是 compiler bounds guard。

# 选择

本轮不改 matcher，只补两个真实 Solidity-generated case 的强 oracle：

- `1490_19504983_72b68e8dfe_38ab68a7adc2`：7 个 checked-bounds，0 skip。
- `1915_19508046_91cdb18983_844dae639fc0`：7 个 checked-bounds，0 skip。

选择原因：

- 它们属于当前最高数量的 7 个 checked-bounds 弱 oracle 档位。
- 两者真实输出都有 semantic helper 和 CFG rewrite。
- 不涉及业务分支判断，rewrite 风险为 0。

不做：

- 不扩展 matcher。
- 不 rewrite `panic_assert`。
- 不一次补完剩余所有弱 oracle。

# 修改

- `test/evm/solidity-patterns/manifest.json:3094`：给 `1490_19504983_72b68e8dfe_38ab68a7adc2` 增加 checked-bounds kinds、panic codes、semantic markers、skip reasons 和 CFG rewrite oracle。
- `test/evm/solidity-patterns/manifest.json:3238`：给 `1915_19508046_91cdb18983_844dae639fc0` 增加同样 oracle。
- `AGENTS.md:225`：补充 CheckedBoundsPass 迭代时 apehex 默认抽样最近批次约 100 个 IR；只有发现新 skip、runner 失败或明确需要扩大覆盖时再扫更多批次。

涉及函数：无。只改测试 manifest 和流程说明。

两个 case 的真实值相同：

- kinds：`array_bounds_memory:2`，`storage_bytes_encoding:1`，`checked_mul:1`，`checked_div:1`，`checked_add:1`，`checked_sub:1`。
- panic codes：`17:3`，`50:2`，`34:1`，`18:1`。
- semantic markers：7。
- skip reasons：0。
- CFG rewrites：7。

# 验证

- `python3 -m json.tool test/evm/solidity-patterns/manifest.json >/tmp/notdec-pattern-manifest-round81.json`：通过。
- 两 case 临时 manifest：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest /tmp/notdec-pattern-round81-two-abs.json --project-root . --workdir /tmp/notdec-patterns-cb-round81-two-abs-final`：2 passed，0 failed。
- `cmake --build ./build --target all`：通过，`ninja: no work to do.`。
- pattern suite：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-patterns/manifest.json --project-root . --workdir /tmp/notdec-patterns-cb-round81-suite`：90 passed，0 failed。
  - checked-bounds：775；semantic markers：774；CFG rewrites：774；skip：`unrecognized_operands:1` / `panic_assert:1`。
- rewrite suite：
  - `python3 test/run_evm_solidity_rewrite_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-rewrite/manifest.json --project-root . --workdir /tmp/notdec-rewrite-cb-round81-suite`：65 passed，0 failed。
  - checked-bounds：626；semantic markers：625；CFG rewrites：625；skip：`unrecognized_operands:1` / `panic_assert:1`。
- CTest：
  - 本轮启动过 `ctest --test-dir build -R 'notdec.evm.solidity_patterns|notdec.evm.solidity_rewrite' --output-on-failure`，但按用户要求不再等待；不作为本轮验证依据。

# 当前状态

- matcher 本轮未变。
- pattern manifest 剩余 checked-bounds 弱 oracle 从 11 个降到 9 个。
- pattern/rewrite suite 的唯一 skip 仍是 `0334` 的 `panic_assert`，当前不安全 rewrite。
- 后续 apehex 验证默认抽样约 100 个 IR，不再每轮扫太多批次。

# 下一轮候选

按真实 case 数量、语义确定性、rewrite 风险、测试成本排序：

1. 7 个 checked-bounds 档位：`0870`、`0657`、`0637`、`0195`、`0128`。真实 case 多；语义确定；rewrite 风险低；测试成本低。
2. `0178`、`0031`、`0460`、`0011_multi_public`。数量较低；语义确定；rewrite 风险低；测试成本低。
3. 继续抽样 apehex 最近批次约 100 个 IR。真实 case 数量未知；语义确定性取决于命中形状；rewrite 风险未知；测试成本低。
4. 归档 `panic_assert` 原始合约/Yul 形状。真实 case 1 个；语义确定性低；rewrite 风险高；测试成本中等。
