# 原始 prompt

> 持续迭代完善 CheckedBoundsPass，但每轮只能基于真实证据扩展，不做猜测式放宽。完成一轮可验证的改进

# 本轮问题

本轮沿用当前真实统计：

- pattern suite：90 个 `.ll`，775 个 checked-bounds，774 个 semantic marker，774 个 CFG rewrite，1 个 skip。
- rewrite suite：65 个 `.ll`，626 个 checked-bounds，625 个 semantic marker，625 个 CFG rewrite，1 个 skip。
- 唯一 skip 是 `0334_19494307_668d201319_1354ce2e324d` 的 `panic_assert`，reason 是 `unrecognized_operands`。
- apehex 最近 batch 输出里暂未命中 checked-bounds。

`panic_assert` 已对照 Solidity：

- `/sn640/solidity/libsolidity/codegen/ExpressionCompiler.cpp:1379`：`assert` 失败走 `appendPanic(PanicCode::Assert)`。
- `/sn640/solidity/libsolidity/codegen/YulUtilFunctions.cpp:304`：assert helper 是 `if iszero(condition) { panic(0x01) }`。

因此本轮不 rewrite panic 0x01。它更像业务 assert，不是能证明安全删除的 compiler bounds guard。

# 选择

本轮不改 matcher，只补 pattern manifest 的强 oracle。

选择原因：

- `0448_19495059_065877b669_4f138305be23`：10 个 checked-bounds，0 skip，是剩余弱 oracle 中数量最高的真实 case。
- `0179_19493473_76bc10e48c_fd3041389134`：9 个 checked-bounds，0 skip，是下一档高数量真实 case。
- 全量验证时又发现 `0121_19493259_63ed382f00_e1eb3485de04` 已有强 oracle 和真实输出不一致：metadata 总数是 8，强 oracle 写成 9。本轮一并按真实 audit 修正。

不做：

- 不扩展 matcher。
- 不碰 `panic_assert` / `Panic(1)`。
- 不一次性补完所有剩余弱 oracle。

# 修改

- `test/evm/solidity-patterns/manifest.json:1089`：给 `0448_19495059_065877b669_4f138305be23` 增加 `expected_checked_bounds_kinds`、`expected_checked_bounds_panic_codes`、`expected_checked_bounds_semantic_markers`、`expected_checked_bounds_skip_reasons`、`expected_checked_bounds_cfg_rewrites`。
- `test/evm/solidity-patterns/manifest.json:3745`：修正 `0121_19493259_63ed382f00_e1eb3485de04` 的 checked-bounds 强 oracle，真实值是 8 个 rewrite，不是 9 个。
- `test/evm/solidity-patterns/manifest.json:4134`：给 `0179_19493473_76bc10e48c_fd3041389134` 增加同样的 checked-bounds 强 oracle。

涉及函数：无。只改测试 manifest。

# 验证

- `python3 -m json.tool test/evm/solidity-patterns/manifest.json >/tmp/notdec-pattern-manifest-round78.json`：通过。
- 三 case 临时 manifest：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest /tmp/notdec-pattern-round78-three-abs.json --project-root . --workdir /tmp/notdec-patterns-cb-round78-three-abs`：3 passed，0 failed。
- `cmake --build ./build --target all`：通过，`ninja: no work to do.`。
- pattern suite：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-patterns/manifest.json --project-root . --workdir /tmp/notdec-patterns-cb-round78-suite-final`：90 passed，0 failed。
  - checked-bounds：775；semantic markers：774；CFG rewrites：774；skip：`unrecognized_operands:1` / `panic_assert:1`。
- rewrite suite：
  - `python3 test/run_evm_solidity_rewrite_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-rewrite/manifest.json --project-root . --workdir /tmp/notdec-rewrite-cb-round78-suite`：65 passed，0 failed。
  - checked-bounds：626；semantic markers：625；CFG rewrites：625；skip：`unrecognized_operands:1` / `panic_assert:1`。
- CTest：
  - `ctest --test-dir build -R 'notdec.evm.solidity_patterns|notdec.evm.solidity_rewrite' --output-on-failure`：2/2 passed，178.71 秒。

# 当前状态

- matcher 本轮未变。
- pattern manifest 剩余 checked-bounds 弱 oracle 从 18 个降到 15 个。
- 剩余 skip 仍只有 `0334_19494307_668d201319_1354ce2e324d` 的 `panic_assert`，暂不 rewrite。

# 下一轮候选

按真实 case 数量、语义确定性、rewrite 风险、测试成本排序：

1. 继续补 pattern manifest 强 oracle：`0176`、`0175` 各 9 个 checked-bounds，`0118` 8 个。真实 case 数量高；语义确定；rewrite 风险低；测试成本低。
2. 补 7 个 checked-bounds 档位的 pattern oracle：`2002`、`1915`、`1490`、`0870`、`0657`、`0637`、`0195`、`0128`。真实 case 多；语义确定；rewrite 风险低；测试成本低。
3. 继续扫 apehex 新 batch。真实 case 数量未知；语义确定性取决于命中形状；rewrite 风险未知；测试成本低。
4. 归档 `panic_assert` 原始 Solidity/Yul 形状。真实 case 1 个；语义确定性低；rewrite 风险高；测试成本中等。
