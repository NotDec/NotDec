# 原始 prompt

> 持续迭代完善 CheckedBoundsPass，但每轮只能基于真实证据扩展，不做猜测式放宽。完成一轮可验证的改进

# 本轮问题

重新统计当前状态：

- pattern manifest 还有 13 个 checked-bounds 弱 oracle。
- apehex batch636 + batch635：66 个 `.ll`，checked-bounds 为 0，没有 skip。
- pattern/rewrite suite 仍只有一个 checked-bounds skip：`0334_19494307_668d201319_1354ce2e324d`，`panic_assert` / `unrecognized_operands`。

`panic_assert` 仍保留，不 rewrite。Solidity `assert` 会生成 `Panic(1)`，当前不能证明它是 compiler bounds guard。

# 选择

本轮不改 matcher，只补两个真实 Solidity-generated case 的强 oracle：

- `0118_19493248_593c688d00_799916048038`：8 个 checked-bounds，0 skip。
- `2002_19510139_923ad4a9d1_4e9ed82fcc5b`：7 个 checked-bounds，0 skip。

选择原因：

- `0118` 是当前弱 oracle 中数量最高的 case。
- `2002` 是下一档 7 个 checked-bounds case 中排序靠前的真实 case。
- 两者真实输出都有 semantic helper 和 CFG rewrite。
- 不涉及业务分支判断，rewrite 风险为 0。

不做：

- 不扩展 matcher。
- 不 rewrite `panic_assert`。
- 不一次补完剩余所有弱 oracle。

# 修改

- `test/evm/solidity-patterns/manifest.json:3142`：给 `2002_19510139_923ad4a9d1_4e9ed82fcc5b` 增加 checked-bounds kinds、panic codes、semantic markers、skip reasons 和 CFG rewrite oracle。
- `test/evm/solidity-patterns/manifest.json:3721`：给 `0118_19493248_593c688d00_799916048038` 增加同样 oracle。

涉及函数：无。只改测试 manifest。

`2002` 真实值：

- kinds：`array_bounds_memory:2`，`storage_bytes_encoding:1`，`checked_mul:1`，`checked_div:1`，`checked_add:1`，`checked_sub:1`。
- panic codes：`17:3`，`50:2`，`34:1`，`18:1`。
- semantic markers：7。
- skip reasons：0。
- CFG rewrites：7。

`0118` 真实值：

- kinds：`array_bounds_memory:4`，`checked_mul:1`，`checked_div:1`，`checked_add:1`，`checked_sub:1`。
- panic codes：`50:4`，`17:3`，`18:1`。
- semantic markers：8。
- skip reasons：0。
- CFG rewrites：8。

实现时曾因 JSON 上下文相同，把 oracle 临时打到相邻 case；全量 pattern suite 通过前已修正，并确认 `0029` 没有 checked-bounds oracle，`1490` 仍是弱 oracle，只有 `0118` 和 `2002` 被补强。

# 验证

- `python3 -m json.tool test/evm/solidity-patterns/manifest.json >/tmp/notdec-pattern-manifest-round80.json`：通过。
- 两 case 临时 manifest：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest /tmp/notdec-pattern-round80-two-abs.json --project-root . --workdir /tmp/notdec-patterns-cb-round80-two-abs-final2`：2 passed，0 failed。
- `cmake --build ./build --target all`：通过，`ninja: no work to do.`。
- pattern suite：
  - `python3 test/run_evm_solidity_patterns_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-patterns/manifest.json --project-root . --workdir /tmp/notdec-patterns-cb-round80-suite-final`：90 passed，0 failed。
  - checked-bounds：775；semantic markers：774；CFG rewrites：774；skip：`unrecognized_operands:1` / `panic_assert:1`。
- rewrite suite：
  - `python3 test/run_evm_solidity_rewrite_suite.py --binary ./build/bin/notdec --manifest test/evm/solidity-rewrite/manifest.json --project-root . --workdir /tmp/notdec-rewrite-cb-round80-suite`：65 passed，0 failed。
  - checked-bounds：626；semantic markers：625；CFG rewrites：625；skip：`unrecognized_operands:1` / `panic_assert:1`。
- CTest：
  - `ctest --test-dir build -R 'notdec.evm.solidity_patterns|notdec.evm.solidity_rewrite' --output-on-failure`：2/2 passed，179.36 秒。

# 当前状态

- matcher 本轮未变。
- pattern manifest 剩余 checked-bounds 弱 oracle 从 13 个降到 11 个。
- pattern/rewrite suite 的唯一 skip 仍是 `0334` 的 `panic_assert`，当前不安全 rewrite。

# 下一轮候选

按真实 case 数量、语义确定性、rewrite 风险、测试成本排序：

1. 7 个 checked-bounds 档位：`1915`、`1490`、`0870`、`0657`、`0637`、`0195`、`0128`。真实 case 多；语义确定；rewrite 风险低；测试成本低。
2. `0178`、`0031`、`0460`、`0011_multi_public`。数量较低；语义确定；rewrite 风险低；测试成本低。
3. 继续扫 apehex 新 batch。真实 case 数量未知；语义确定性取决于命中形状；rewrite 风险未知；测试成本低。
4. 归档 `panic_assert` 原始合约/Yul 形状。真实 case 1 个；语义确定性低；rewrite 风险高；测试成本中等。
