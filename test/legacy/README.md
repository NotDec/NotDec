## Legacy Test Material

`test/legacy/` keeps older ad-hoc workspaces and experiment data that are still
useful for reference, but are not treated as current `ctest` suites or golden
oracles.

- `wasm/`
  - Historical wasm-centric scripts, datasets, and generated artifacts.
  - Use these as migration/reference material, not as the canonical regression
    layout.
